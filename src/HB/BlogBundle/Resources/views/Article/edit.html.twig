{% extends 'HBBlogBundle::layout.html.twig' %}

{% block content -%}
    <h3>Article edit (détaillé2)</h3>

{#    {{ form(edit_form) }}  #}
    
 {{ form_start(edit_form) }}
    {{ form_errors(edit_form) }}

    {{ form_row(edit_form.title, { 'attr': {'class': 'ici'} }) }}   
    {{ form_row(edit_form.content) }}
    
    {{ form_row(edit_form.author, { 'attr': {'class': 'ici'} }) }}

    {# affiche un widget, mais y ajoute une classe "foo" #}
 {#   {{ form_widget(edit_form.title, { 'attr': {'class': 'ici'} }) }}  #}
    {{ form_label(edit_form.title,'Le Titre', {'label_attr': {'class':'gras'}}) }} 

 
{# ne pas rendre les champs non définis #}
{# {{ form_end(edit_form, {'render_rest': false}) }} #}

        <ul class="record_actions">
    <li>
        <a href="{{ path('article') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
 
    {{ form_end(edit_form) }} 


{% endblock %}