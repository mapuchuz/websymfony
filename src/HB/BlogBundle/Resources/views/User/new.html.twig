{% extends 'HBBlogBundle::layout.html.twig' %}

{% block content -%}
    <h3>User creation</h3>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
